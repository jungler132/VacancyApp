# Copy to keystore.properties (gitignored). The prebuild plugin
# plugins/withAndroidReleaseSigning.js wires this into release signing.
# If the file is missing, local release still uses the debug keystore.
storeFile=upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
